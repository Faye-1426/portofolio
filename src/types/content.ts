export type TechItem = {
  name: string;
  icon: string; // public path
};

export type TimelineItem = {
  year: string;
  title: string;
  description: string;
};

export type AbilityItem = {
  title: string;
  description: string;
};
